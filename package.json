{
  "name": "svelte-app",
  "version": "0.0.408",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "test": "echo skipping...",
    "predeploy": "yarn build; yarn version --patch",
    "deploy": "cd dist; ln -s ../.now .vercel; vc --prod",
    "cc": "prettier --check src/*/*.{css,js,svelte}",
    "format": "prettier --write src/*/*.{css,js,svelte}",
    "spell": "cspell 'src/**/*.{js,svelte}'",
    "open": "open https://s4e-proofs.vercel.app"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "2.4.2",
    "cspell": "6.31.2",
    "prettier": "3.0.0",
    "prettier-plugin-svelte": "3.0.0",
    "rangetouch": "2.0.1",
    "svelte": "4.1.0",
    "vite": "4.4.4"
  }
}
